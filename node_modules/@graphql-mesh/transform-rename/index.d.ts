import { GraphQLSchema } from 'graphql';
import { MeshTransform, YamlConfig, MeshTransformOptions } from '@graphql-mesh/types';
import { Request, Result } from '@graphql-tools/utils';
export default class RenameTransform implements MeshTransform {
    private transforms;
    constructor(options: MeshTransformOptions<YamlConfig.RenameTransformObject[]>);
    transformSchema(schema: GraphQLSchema): GraphQLSchema;
    transformRequest(request: Request): Request;
    transformResult(result: Result): any;
}
